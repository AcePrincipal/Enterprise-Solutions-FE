function optionReducer(state = {}, action){
    switch(action.type){
        case "A":
            return currentState = true;
        default:
            return currentState;
    }
}

export default optionReducer;